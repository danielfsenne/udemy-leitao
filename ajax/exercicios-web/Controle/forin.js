const nota = [6.7, 9.1, 8.4, 7,4, 3.6]

for(let i in nota) {
    console.log(i, nota[i])
}
const pessoa = {
    nome:'Ana',
    idade:32,
    peso:64
    
}
for(atributo in pessoa) {
    console.log(`${atributo} = ${pessoa[atributo]}`)
}
console.log(atributo)